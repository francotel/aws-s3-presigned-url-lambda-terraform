# -----------------------------------
# 🔍 Trivy configuration
# -----------------------------------

severity:
  - HIGH
  - CRITICAL

exit-code: 1

ignore-unfixed: true

scan:
  scanners:
    - vuln
    - misconfig
    - secret

vulnerability:
  type:
    - os
    - library

format: table